# Install a mutually compatible torch/torchvision build for your CUDA runtime.
# These are compatibility ranges, not a fully runtime-validated lockfile.
torch>=2.1,<3
torchvision>=0.16,<1
numpy>=1.24,<3
Pillow>=9
pytorch-msssim>=1.0,<2
tensorboardX>=2.6
matplotlib>=3.7
tqdm>=4.65
